extern printi(x i32) i32
extern println(x str) i32

func test(x i32, y i32) i32 {
    printi(x)
    x = x + y
    printi(x)
    z = 4
    x = x + z
    printi(x)
    0
}

func main() i32 {
    test(1, 2)
    0
}

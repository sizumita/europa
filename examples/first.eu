extern printi(x i32) i32
extern println(x str) i32
extern same(x str, y str) bool

func test(x str) i32 {
    if x == "abc" {
        println("same")
    } else {
        println("diff")
    }
}

func main() i32 {
    printi(String.compare("abc", "abc"))
    test("abc")
    0
}
